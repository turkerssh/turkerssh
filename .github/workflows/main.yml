name: Update readme

on:
 schedule:
  - cron: "*/15 * * * *"
 workflow_dispatch:

jobs:
 main:
  name: ✨ README Stats
  runs-on: ubuntu-latest
  strategy:
   fail-fast: false
  steps:
   - name: 🧱 Checkout repository
     uses: actions/checkout@v3
     with:
      fetch-depth: 1
   - name: 🔩 Install Deno 
     uses: denoland/setup-deno@v1
     with:
       deno-version: v1.x
   - name: ✨ Generate README.md
     run: deno task start
     env:
      GITHUB_TOKEN: ${{ secrets.GH_TOKEN }}
      WAKATIME_API_KEY: ${{ secrets.WAKATIME_API_KEY }}
   - name: 🚀 Save changes
     run: |
      if [[ "$(git status --porcelain)" != "" ]]; then
      git config user.name turkwr
      git config user.email 63150613+turkwr@users.noreply.github.com
      git add .
      git commit -m "[Bot] Update blog feed and activity"
      git push -f
      fi
